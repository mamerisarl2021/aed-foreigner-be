meta {
  name: 10) List VALIDATION_AGENT|REJET_AGENT (responsable)
  type: http
  seq: 10
}

get {
  url: {{gateway_url}}/api/v1/enrolements?statut=VALIDATION_AGENT|REJET_AGENT&per_page=10
  body: none
  auth: bearer
}

auth:bearer {
  token: {{supervisor_token}}
}

script:post-response {
  test('Status 200', () => expect(res.getStatus()).to.equal(200));
}
