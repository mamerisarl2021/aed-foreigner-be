meta {
  name: 4) Agent instruction VALIDATION_AGENT
  type: http
  seq: 4
}

patch {
  url: {{gateway_url}}/api/v1/enrolements/{{demande_id}}/instruction
  body: json
  auth: bearer
}

auth:bearer {
  token: {{staff_token}}
}

body:json {
  {
    "statut": "VALIDATION_AGENT"
  }
}

script:post-response {
  test('Status 200', () => expect(res.getStatus()).to.equal(200));
}
