meta {
  name: 12) Validation APPROUVEE (responsable)
  type: http
  seq: 12
}

patch {
  url: {{gateway_url}}/api/v1/enrolements/{{demande_id}}/validation
  body: json
  auth: bearer
}

auth:bearer {
  token: {{supervisor_token}}
}

body:json {
  {
    "decision": "APPROUVEE"
  }
}

script:post-response {
  test('Status 200', () => expect(res.getStatus()).to.equal(200));
}
