meta {
  name: 6) Responsable validation APPROUVEE
  type: http
  seq: 6
}

patch {
  url: {{gateway_url}}/api/v1/enrolements/{{demande_id}}/validation
  body: json
  auth: bearer
}

auth:bearer {
  token: {{staff_token}}
}

body:json {
  {
    "decision": "APPROUVEE"
  }
}

script:post-response {
  test('Status 200', () => expect(res.getStatus()).to.equal(200));
}
