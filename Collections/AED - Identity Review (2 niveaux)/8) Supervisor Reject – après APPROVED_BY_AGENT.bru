meta {
  name: 8) Supervisor Reject – après APPROVED_BY_AGENT
  type: http
  seq: 8
}

post {
  url: {{base_url}}/api/management/identity-reviews/{{identity_id_supervised}}/supervisor/reject
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{supervisor_token}}
}

body:json {
  {
    "stage": "STRUCTURE",
    "reasons": ["Pièce manquante", "IFU invalide"],
    "comments": "Ajoutez le justificatif RCCM valide."
  }
}

script:post-response {
  test('200', () => expect(res.getStatus()).to.equal(200));
}

settings {
  encodeUrl: true
}
