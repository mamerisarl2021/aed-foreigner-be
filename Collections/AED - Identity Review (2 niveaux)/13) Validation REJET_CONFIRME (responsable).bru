meta {
  name: 13) Validation REJET_CONFIRME (responsable)
  type: http
  seq: 13
}

patch {
  url: {{gateway_url}}/api/v1/enrolements/{{demande_id}}/validation
  body: json
  auth: bearer
}

auth:bearer {
  token: {{supervisor_token}}
}

body:json {
  {
    "decision": "REJET_CONFIRME"
  }
}

script:post-response {
  test('Status 200', () => expect(res.getStatus()).to.equal(200));
}
