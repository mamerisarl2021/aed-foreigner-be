meta {
  name: 1) List EN_ATTENTE (agent)
  type: http
  seq: 1
}

get {
  url: {{gateway_url}}/api/v1/enrolements?statut=EN_ATTENTE&per_page=10
  body: none
  auth: bearer
}

auth:bearer {
  token: {{staff_token}}
}

script:post-response {
  test('Status 200', () => expect(res.getStatus()).to.equal(200));
}
