meta {
  name: 3) Show - Détail (agent)
  type: http
  seq: 3
}

get {
  url: {{gateway_url}}/api/v1/enrolements/{{demande_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{staff_token}}
}

script:post-response {
  test('Status 200', () => expect(res.getStatus()).to.equal(200));
}
