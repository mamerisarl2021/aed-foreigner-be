meta {
  name: 3) Show - Détail (agent)
  type: http
  seq: 3
}

get {
  url: {{base_url}}/api/management/identity-reviews/{{identity_id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{agent_token}}
}

script:post-response {
  test('200', () => expect(res.getStatus()).to.equal(200));
}

settings {
  encodeUrl: true
}
