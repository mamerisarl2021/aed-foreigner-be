meta {
  name: 3) Send phone OTP
  type: http
  seq: 3
}

post {
  url: {{base_url}}/api/v1/morale/enrollments/{{morale_enrollment_id}}/send-phone-otp
  auth: bearer
}

auth:bearer {
  token: {{client_token}}
}

script:post-response {
  test('200', () => expect(res.getStatus()).to.equal(200));
}
